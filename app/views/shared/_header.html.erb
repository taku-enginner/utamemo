<header class="p-4 bg-slate-950 sticky top-0 z-50 flex justify-between items-center pb-2">
  <!-- オーバーレイ --> 
  <div id="menu-overlay" class="fixed inset-0 bg-black opacity-0 pointer-events-none transition-opacity duration-200"></div>
  <div class="inline-block text-3xl font-bold text-pink-500 ">
    <%= link_to "UTAMEMO", root_path , data: {turbo: false} %>
  </div>
  <!-- メニューボタンと検索アイコン -->
  <div class="relative flex flex-col items-center">
    <div class="flex flex-row items-center space-x-2 mb-2">
      <!-- 虫眼鏡アイコン -->
      <!-- Open the modal using ID.showModal() method -->
      <div class=" flex items-center justify-center">
        <dialog id="my_modal_2" class="modal">
          <div class="modal-box text-center bg-slate-950 border-2 border-pink-600">
            <h3 class="text-lg font-bold text-white">歌詞を検索</h3>
            <%= form_with url: musixmatch_search_path, method: :get do |form| %>
              <div class="flex flex-col space-y-4">
                <div>
                  <%= form.text_field :artist_name, required: true, placeholder: "アーティスト名", class: "text-lg p-2 mt-2 rounded-lg border-2 border-pink-600 bg-gray-300 text-purple-600 placeholder-purple-500 focus:outline-none focus:ring-2 focus:ring-purple-500" %>
                </div>
                <div>
                  <%= form.text_field :title, required: true, placeholder: "曲名", class: "text-lg p-2 rounded-lg border-2 border-pink-600 bg-gray-300 text-purple-600 placeholder-purple-500 focus:outline-none focus:ring-2 focus:ring-purple-500" %>            
                </div>
                
                <%= form.submit "検索", class: "bg-pink-600 text-white font-bold py-2 px-4 rounded-lg hover:bg-pink-700 transition-shadow shadow-md hover:shadow-lg" %>
              </div>
            <% end %>
          </div>
          <form method="dialog" class="modal-backdrop">
            <button>close</button>
          </form>
        </dialog>
      </div>
      <!-- メニューアイコン -->
      <button
        class="btn bg-blue-500 text-white rounded-lg px-4 py-2 flex items-center justify-center"
        id="menu-button"
      >
        ☰
      </button>
    </div>
    <!-- アカウント名 -->
    <div>
      <% if user_signed_in? %>
        <div class="text-sm text-pink-500 font-bold" %>
          <%= current_user.nickname %>
        </div>
      <% else %>
        <div class="text-sm text-pink-500 font-bold">ゲスト</div>
      <% end %>
    </div>

    <!-- メニュー (デフォルトで非表示) -->
    <ul
      class="menu absolute right-0 top-full mt-2 bg-blue-500 p-2 shadow-md rounded-lg w-48 z-50 text-white hidden transition-opacity duration-200"
      id="menu-content"
    >
      <% if user_signed_in? %>
        <li><%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete}, class: "block px-4 py-2 hover:bg-blue-600" %></li>
        <li><%= link_to "マイページ", mypage_index_path, class: "block px-4 py-2 hover:bg-blue-600" %></li>
      <% else %>
        <li><%= link_to "ログイン", new_user_session_path, class: "block px-4 py-2 hover:bg-blue-600" %></li>
        <li><%= link_to "新規会員登録", new_user_registration_path, class: "block px-4 py-2 hover:bg-blue-600" %></li>
      <% end %>
      <li>
        <button class="text-white px-4 py-2 hover:bg-blue-600" onclick="my_modal_2.showModal()">新規メモ作成🔍</button>
      </li>
      <li><%= link_to "新着メモ一覧", memos_path, class: "block px-4 py-2 hover:bg-blue-600" %></li>
      <!--
      <li><%= link_to "お問い合わせ", "#", class: "block px-4 py-2 hover:bg-blue-600" %></li>
      <li><%= link_to "利用規約", "#", class: "block px-4 py-2 hover:bg-blue-600" %></li>
      <li><%= link_to "プライバシーポリシー", "#", class: "block px-4 py-2 hover:bg-blue-600" %></li>
      -->
    </ul>
  </div>
</header>
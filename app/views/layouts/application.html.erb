<!DOCTYPE html>
<html>
  <head>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=M+PLUS+Rounded+1c&display=swap" rel="stylesheet">
    <title><%= content_for(:title) || "Myapp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%= javascript_pack_tag "application" %>

    <!-- 本番環境でReactコンポーネントがうまく初期描画されないので、ページ全体でturboを無効化してみる -->
    <meta name="turbo-visit-control" content="reload">

  </head>

  <body class="flex flex-col min-h-screen bg-zinc-700">
    
    <%= render "shared/header" %>
    <%= render "shared/flash" %>
    <div class="flex-1 min-h-screen relative"><%= yield %></div>
    <%= render "shared/footer" %>

  </body>
</html>
